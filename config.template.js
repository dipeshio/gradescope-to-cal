/**
 * OAuth Configuration for GradescopeToCal
 * 
 * SETUP: Copy this file to 'config.js' and add your own credentials.
 * 
 * How to get credentials:
 * 1. Go to https://console.cloud.google.com/
 * 2. Create a new project (or select existing)
 * 3. Enable "Google Tasks API"
 * 4. Go to "Credentials" → "Create Credentials" → "OAuth client ID"
 * 5. Choose "Chrome Extension" as application type
 * 6. Add your extension ID (find it at chrome://extensions/)
 * 7. Copy the Client ID below
 */

const CONFIG = {
    // Your OAuth 2.0 Client ID from Google Cloud Console
    CLIENT_ID: 'YOUR_CLIENT_ID_HERE.apps.googleusercontent.com'
};
